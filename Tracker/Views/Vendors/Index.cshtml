@using Tracker.Models;

<h1>Your Vendors</h1>

@if (@Model.Count == 0)
{
    <h3>You must add vendors first to view your list.</h3>
}

@foreach (Vendor vendors in Model)
{
    <h3>Vendor name: @vendors.Name Vendor description: @vendors.Description</h3>
}